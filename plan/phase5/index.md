# Phase 5: 全面完善

## 概述

Phase 1-4 完成了从基础架构到进阶功能的全栈开发。Phase 5 的目标是**全面完善所有已有功能**，
补齐 pi RPC 协议中定义但前端未接入的能力，打磨交互细节，提升完整体验。

## 步骤链路

```
Step 5.1 → Step 5.2 → Step 5.3 → Step 5.4 → Step 5.5
  │           │           │           │           │
  │           │           │           │           └─ 消息反馈 + 滚动优化
  │           │           │           └─ 输入框增强（斜杠菜单 + 工具栏 + set_editor_text）
  │           │           └─ 消息操作右键菜单
  │           └─ 消息复制增强（整条/代码块/片段）
  └─ 斜杠命令 / 菜单
```

## Todo 清单

- [x] **5.1** 斜杠命令菜单 — 输入 `/` 弹出命令列表，调用 `get_commands`
- [x] **5.2** 消息复制增强 — 右键复制整条 AI 回复、单个代码块、选中文本
- [x] **5.3** 消息操作右键菜单 — 用户消息（编辑/Fork/复制/Clone）、AI 消息（复制/重新生成）
- [x] **5.4** 输入框增强 — 斜杠菜单 + 悬浮工具栏（加粗/斜体/代码/引用）+ set_editor_text
- [x] **5.5** 消息反馈 — 点赞/踩 + 重新生成 + 滚动到底部优化
